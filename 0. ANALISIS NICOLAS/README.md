# Análisis exploratorio de Nicolás Jácome

Esta carpeta contiene el trabajo exploratorio de Nicolás Jácome (coautor, GitHub `njacomev`), separado del resto del análisis asistido por IA que vive en `0. ANALISIS INICIAL IA/`. Nunca se validó ni se citó en ninguna cifra reportada del proyecto.

`construir_base_analitica_nicolas.R` y `Analisis_EAM_Markdown.Rmd` tienen cada uno una corrección de ruta de una sola línea hecha posteriormente por Julio (`source(...)` apuntando a la nueva ubicación de `_utils_proyecto.R`/`analisis_eam_eac.R` tras la reorganización de `3. SCRIPTS/` en subcarpetas) — es un fix mecánico para que los scripts siguieran corriendo fuera de su ubicación original, no un cambio de lógica ni de contenido analítico.
